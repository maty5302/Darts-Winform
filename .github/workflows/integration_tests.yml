name: .NET Core Integration Tests

on:
  push:
    branches: [ master , dev , dev_*]
  pull_request:
    branches: [ master , dev ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore Tests/Integration.Tests/Integration.Tests.csproj

    - name: Build
      run: dotnet build Tests/Integration.Tests/Integration.Tests.csproj --no-restore

    - name: Run Integration.Tests
      run: dotnet test Tests/Integration.Tests/Integration.Tests.csproj --no-build --verbosity normal
